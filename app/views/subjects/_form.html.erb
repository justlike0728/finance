<%= simple_form_for(@subject) do |f| %>
	<%= f.input :title, label: '費別' %>
	<%= f.submit class: 'btn btn-primary btn-sm' %>
	<%= link_to '回費別總表', subjects_path, class: 'btn btn-info btn-sm' %>
<% end %>